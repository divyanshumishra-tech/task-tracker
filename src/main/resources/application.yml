spring:
  application:
    name: task-tracker
  datasource:
    url: jdbc:mysql://localhost:3306/task_tracker?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "@Yashmis1009"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update      
    show-sql: true          
    properties:
      hibernate:
        format_sql: true    
        dialect: org.hibernate.dialect.MySQLDialect
        highlight_sql: true